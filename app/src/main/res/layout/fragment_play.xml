<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/common_padding">

    <FrameLayout
        android:id="@+id/fl_seek_bar_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_margin="@dimen/common_margin">

    </FrameLayout>


    <LinearLayout
        android:id="@+id/btn_group"
        android:layout_width="match_parent"
        android:layout_height="@dimen/play_button_group_height"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_margin="@dimen/common_margin">

        <ImageView
            android:id="@+id/iv_cycle"
            android:layout_width="0dp"
            android:layout_height="@dimen/play_small_button_size"
            android:layout_weight="2"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_replay_no"/>

        <ImageView
            android:id="@+id/iv_previous"
            android:layout_width="0dp"
            android:layout_height="@dimen/play_small_button_size"
            android:layout_weight="2"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_skip_previous" />

        <ImageView
            android:id="@+id/iv_play"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="3"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_play_arrow"/>

        <ImageView
            android:id="@+id/iv_next"
            android:layout_width="0dp"
            android:layout_height="@dimen/play_small_button_size"
            android:layout_weight="2"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_skip_next"/>

        <ImageView
            android:id="@+id/iv_random"
            android:layout_width="0dp"
            android:layout_height="@dimen/play_small_button_size"
            android:layout_weight="2"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_shuffle_off"/>

    </LinearLayout>

</LinearLayout>